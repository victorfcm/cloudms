{% extends '::base.html.twig' %}

{% block body -%}
    <h1>PostRelashionship creation</h1>

    <form action="{{ path('post_relashionship_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('post_relashionship') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
