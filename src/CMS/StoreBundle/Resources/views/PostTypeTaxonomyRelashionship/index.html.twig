{% extends '::base.html.twig' %}

{% block body -%}
    <h1>PostTypeTaxonomyRelashionship list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Posttypeid</th>
                <th>Taxonomyid</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('post_type_tax_rl_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.postType }}</td>
                <td>{{ entity.taxonomy }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('post_type_tax_rl_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('post_type_tax_rl_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('post_type_tax_rl_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
