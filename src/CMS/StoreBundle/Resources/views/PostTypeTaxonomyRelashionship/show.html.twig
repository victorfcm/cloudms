{% extends '::base.html.twig' %}

{% block body -%}
    <h1>PostTypeTaxonomyRelashionship</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Posttypeid</th>
                <td>{{ entity.postTypeId }}</td>
            </tr>
            <tr>
                <th>Taxonomyid</th>
                <td>{{ entity.taxonomyId }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('post_type_tax_rl') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('post_type_tax_rl_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('post_type_tax_rl_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
