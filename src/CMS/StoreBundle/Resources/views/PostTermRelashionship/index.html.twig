{% extends '::base.html.twig' %}

{% block body -%}
    <h1>PostTermRelashionship list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Term</th>
                <th>Post</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('posttermrelashionship_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.term }}</td>
                <td>{{ entity.post }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('posttermrelashionship_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('posttermrelashionship_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('posttermrelashionship_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
