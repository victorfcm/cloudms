{% extends '::base.html.twig' %}

{% block body -%}
    <h1>PostTermRelashionship</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Termid</th>
                <td>{{ entity.termId }}</td>
            </tr>
            <tr>
                <th>Postid</th>
                <td>{{ entity.postId }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('posttermrelashionship') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('posttermrelashionship_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('posttermrelashionship_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
