{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TermTaxonomyRelashionship</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Termid</th>
                <td>{{ entity.term }}</td>
            </tr>
            <tr>
                <th>Taxonomyid</th>
                <td>{{ entity.taxonomy }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('term-taxonomy') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('term-taxonomy_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('term-taxonomy_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
